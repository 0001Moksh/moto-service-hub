# Production Environment Variables (Next.js public build config)
# This file is safe to commit - it contains NO secrets
# All secrets must be set in Vercel's environment settings dashboard

# Supabase Public Configuration
NEXT_PUBLIC_SUPABASE_URL=https://brnsimoaoxuhpxzrfpcg.supabase.co

# App Metadata
NEXT_PUBLIC_APP_NAME="Moto Service Hub"
NEXT_PUBLIC_ENV="production"
NEXT_PUBLIC_HEALTH_CHECK_INTERVAL="30000"

# Admin Contact (Public)
NEXT_PUBLIC_ADMIN_MAIL="nofackai@gmail.com"
NEXT_PUBLIC_ADMIN_PASSWORD="0987654321"

# OCR Provider
OCR_PROVIDER="tesseract"

# IMPORTANT: Set these secrets in Vercel's Environment Variables dashboard:
# https://vercel.com/mokshs-projects-02b21b2e/moto-service-hub/settings/environment-variables
#
# Required for Production:
# - NEXT_PUBLIC_SUPABASE_ANON_KEY (from Supabase)
# - SUPABASE_SERVICE_ROLE_KEY (from Supabase - SECRET KEY)
# - SMTP_HOST (gmail.com)
# - SMTP_PORT (587)
# - SMTP_USER (sender email)
# - SMTP_PASS (app password)
